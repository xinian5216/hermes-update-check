# Runtime dependencies (pyproject.toml is the source of truth; this file is a
# convenience for `pip install -r requirements.txt` on a bare VPS).
PyYAML>=6.0
rich>=13.0

# Development / tests:
# pytest>=7.4
